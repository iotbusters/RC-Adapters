// #pragma once

// #include <Arduino.h>
// #include <Utils.h>

// typedef uint10_t uint8_t;

// class Adc
// {
// private:
//     const size_t *pins;

// public:
//     const size_t count;

//     Adc(const size_t *pins, const size_t count) : pins(pins), count(count) {}

//     void begin() const;

//     bool read8Bit(const uint8_t slaveAddress, uint8_t &value) const;
//     bool read12Bit(const uint8_t slaveAddress, uint12_t &value) const;
//     bool read16Bit(const uint8_t slaveAddress, uint16_t &value) const;
// };
